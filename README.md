# Selenium-Form-Automation-Python-
This project demonstrates automating a web form submission using Selenium WebDriver with Python. The script opens a signup form, fills in user details (first name, last name, email), submits the form, and then closes the browser.
